export const DB_TYPE: {
  TRINO: string;
  MYSQL: string;
  ORACLE: string;
  SQL_SERVER: string;
  ODBC: string;
} = {
  TRINO: "trino",
  MYSQL: "mysql",
  ORACLE: "oracle",
  SQL_SERVER: "sqlserver",
  ODBC: "odbc",
};
