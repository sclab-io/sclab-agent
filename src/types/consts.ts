export const DB_TYPE: {
  TRINO: string;
  MYSQL: string;
  POSTGRES: string;
  ORACLE: string;
  SQL_SERVER: string;
  ODBC: string;
} = {
  TRINO: 'trino',
  MYSQL: 'mysql',
  POSTGRES: 'postgres',
  ORACLE: 'oracle',
  SQL_SERVER: 'sqlserver',
  ODBC: 'odbc',
};
